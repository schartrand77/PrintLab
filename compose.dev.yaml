services:
  printlab:
    container_name: printlab-dev
    build:
      args:
        ORCA_LINUXDIR_URL: ${ORCA_LINUXDIR_URL:-}
    image: printlab-dev:local
    environment:
      LOG_LEVEL: INFO
      REQUIRE_AUTH: "false"
      ADMIN_PASSWORD_HASH: ""
      SESSION_COOKIE_SECURE: "false"
